{
  "name": "babyprogress",
  "version": "0.5.0",
  "description": "First FB Messenger bot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "klaudioz",
  "license": "ISC",
  "dependencies": {
    "bootbot": "1.0.6", /* FB bot */
    "node-fetch": "1.6.0", /* fetch */
    "cron": "1.1.0", /* cron the message */
    "moment": "2.14.1", /* date handling */
    "traverse":"0.6.6", /* language selection */
    "chrono-node":"1.2.4", /*parsing dates */
    "date-diff":"0.1.3", /* date calculation */
    "nodemon":"1.10.2" /* reload information before reload*/
  }
}
